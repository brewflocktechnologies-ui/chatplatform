spring:
  jpa:
    # show-sql stays off even in dev: full statements leak schema/parameter
    # shapes into logs, and the slow-query threshold below already surfaces
    # the queries worth looking at.
    properties:
      hibernate:
        generate_statistics: true
        session.events.log.LOG_QUERIES_SLOWER_THAN_MS: 200
