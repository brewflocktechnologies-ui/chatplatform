version: "2"
linters:
  default: standard
  settings:
    errcheck:
      check-type-assertions: true
issues:
  # Generated code is excluded by golangci-lint's default generated-file
  # detection (sqlc/protoc emit the standard header).
  max-issues-per-linter: 0
  max-same-issues: 0
