meta {
  name: 03 Get Tenant
  type: http
  seq: 3
}

get {
  url: {{chatUrl}}/api/v1/tenants/{{chat_tenant_id}}
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

assert {
  res.status: eq 200
}
