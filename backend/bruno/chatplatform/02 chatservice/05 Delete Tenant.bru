meta {
  name: 05 Delete Tenant
  type: http
  seq: 5
}

delete {
  url: {{chatUrl}}/api/v1/tenants/{{chat_tenant_id}}
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

assert {
  res.status: eq 204
}
