meta {
  name: 05 Delete Tenant (via gRPC)
  type: http
  seq: 5
}

delete {
  url: {{bffUrl}}/api/v1/tenants/{{bff_tenant_id}}
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

assert {
  res.status: eq 204
}
