meta {
  name: 03 Get Tenant (via gRPC)
  type: http
  seq: 3
}

get {
  url: {{bffUrl}}/api/v1/tenants/{{bff_tenant_id}}
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

assert {
  res.status: eq 200
}
